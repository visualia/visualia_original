export const onError = (error) => {
  console.error(error);
  return true;
};

export const onWarning = (error) => {
  console.error(error);
  return true;
};

export const onCompilerError = (error) => {
  console.error(error);
  return true;
};
